name: Runner Probe

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Prove runner works
        run: |
          echo "PMCN_RUNNER_OK"
          uname -a
